#!/bin/bash

# File Number Modify
# ファイル名の連番を変更する。
# 第1引数として連番、第2引数としてファイル名を指定する。
# `-d 桁数`オプションで、連番桁数を指定できる。（デフォルトは`2`）

# オプションの解析
digit=2
while getopts f:d: opt; do
  case $opt in
  d) digit="$OPTARG" ;;
  *) exit 1 ;;
  esac
done
shift $((OPTIND - 1))

mv "$2" "$(fn-path -d "$digit" "$1" "$2")"
